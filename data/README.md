# Data directory

Raw datasets are **not** included in this repository. See the Datasets table in
the top-level README for download locations.

The experiment runners in `scripts/` expect dataset paths as described in the
Environment note in the top-level README.
