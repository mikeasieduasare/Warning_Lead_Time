# Results directory

`.npz` files produced by the experiment runners in `scripts/` land here, and are
consumed by the scripts in `analysis/`.

These files are excluded from version control via `.gitignore` because of their
size; regenerate them by running Stage 1 as described in the top-level README.
